{
  "manifest_version": 3,
  "name": "SoundCloud rAF Throttle",
  "version": "2.0",
  "description": "Caps SoundCloud's SMIL spinner and requestAnimationFrame loops to ~30fps, so the page stops forcing a full style/layout/paint/composite on every display frame.",
  "content_scripts": [
    {
      "matches": [
        "*://*.soundcloud.com/*"
      ],
      "js": [
        "throttle.js"
      ],
      "run_at": "document_start",
      "world": "MAIN",
      "all_frames": true
    }
  ]
}
